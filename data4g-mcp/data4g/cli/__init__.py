"""`data4g` setup CLI.

Not to be confused with `data4g-mcp` — the CLI is only for bootstrapping
(writing MCP configs, running diagnostics). All sync work happens through
the MCP server, not the CLI.
"""
